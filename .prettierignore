pnpm-lock.yaml
node_modules
dist
*.tsbuildinfo

# Nx local cache (local-only, never formatted)
.nx

# CI-only checkout of ai-plugin-marketplace/template at the pinned revision (see
# .github/template-repo.rev, .github/workflows/ci.yml) — fixture data for the gemini/kiro bundle
# parity suites, not source this repo owns or formats.
.template-repo

# Generated API surface (API Extractor / API Documenter)
packages/*/docs
packages/*/api-report
packages/*/temp

# Vendored third-party spec fixture (SARIF 2.1.0 JSON Schema) — kept byte-identical to the OASIS
# source so it stays diffable against upstream; see lint-sarif-schema.test.ts.
packages/cli/src/lint/sarif-2.1.0.schema.json

# Embedded AI plugin marketplace (this repo dogfoods its own toolchain). This content is authored
# and generated under `aipm` and validated by `aipm validate`'s freshness check — formatting the
# generated artifacts would fight that check. The toolchain's own source under packages/ is still
# formatted normally.
agent-plugins
.aipm
.claude-plugin
.cursor-plugin
.agents
/gemini-extension.json
/GEMINI.md
/POWER.md
/mcp.json
/skills
/commands
/steering
