{
  "$schema": "https://unpkg.com/syncpack@15.3.1/dist/schema.json",
  "versionGroups": [
    {
      "label": "Internal @ai-plugin-marketplace/* packages must use the workspace protocol",
      "dependencies": ["@ai-plugin-marketplace/**"],
      "dependencyTypes": ["prod", "dev"],
      "pinVersion": "workspace:^"
    }
  ],
  "semverGroups": [
    {
      "label": "Internal workspace deps are pinned via the workspace protocol, not a semver range",
      "dependencies": ["@ai-plugin-marketplace/**"],
      "isIgnored": true
    },
    {
      "label": "Use caret ranges for all external dependencies",
      "dependencies": ["**"],
      "dependencyTypes": ["prod", "dev"],
      "range": "^"
    }
  ]
}
